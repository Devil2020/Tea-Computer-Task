package sa.com.morse.teacomputertask.local

/*
import com.mohammedmorse.differencebetweendatabaselibraries.gateways.Game
import com.mohammedmorse.differencebetweendatabaselibraries.gateways.LocaleGateway

class RoomClient (val db : RoomContract) : LocaleGateway {

    override suspend fun clearDB() : Int{
      return  db.clearCachedGamesItems()
    }

    suspend override fun addGame(game: Game): Long {
        return db.addGameItem(game)
    }

    suspend override fun addGames(games: ArrayList<Game>): ArrayList<Long> {
        return db.addGamesItems(games) as ArrayList<Long>
    }

    suspend override fun getGames(): ArrayList<Game> {
        return db.getGamesItems() as ArrayList<Game>
    }

    suspend override fun getGamesCount(): Int {
        return db.getGamesCount()
    }

    suspend override fun removeGame(gameId: Int) : Int{
       return db.deleteGameItemById(gameId)
    }
}*/

